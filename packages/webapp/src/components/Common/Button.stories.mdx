import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { Button } from './Button.component'
import { action } from '@storybook/addon-actions'

<Meta title="MDX/Checkbox" component={Button} />

# Button

This is the documentation for the `Button` used in this app, it has the params`onClick`, `disabled`

<Canvas>
  <Story name="all button">
    <Button onClick={action('clicked normal')}>Normal Button</Button>
    <Button onClick={action('clicked disabled')} disabled>
      disabled Button
    </Button>
  </Story>
</Canvas>
