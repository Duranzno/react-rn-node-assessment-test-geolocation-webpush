{
  "name": "sosafe-assesment-test-shared-data",
  "version": "1.0.1",
  "license": "MIT",
  "private": "true",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "yarn eslint --fix -c .eslintrc.js src/index.ts",
    "build": "tsc --build",
    "start": "yarn build && node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^3.7.2"
  }
}